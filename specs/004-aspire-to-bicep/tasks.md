# Tasks: Aspire-to-Bicep PoC CLI Command

**Input**: Design documents from `/specs/004-aspire-to-bicep/`
**Prerequisites**: plan.md ✓, spec.md ✓, research.md ✓, data-model.md ✓, contracts/cli-contract.md ✓, quickstart.md ✓

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no dependencies on incomplete tasks)
- **[Story]**: Which user story this task belongs to (e.g., US1, US2, US3, US4)
- Include exact file paths in descriptions

## Path Conventions

All source code paths are relative to the `radius` repository root (`github.com/radius-project/radius`). The project extends the existing CLI codebase. New code lives under `pkg/cli/cmd/bicep/generate/` following the established one-package-per-command pattern.

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Create package directory structure for the new command

- [X] T001 Create package directory structure: `pkg/cli/cmd/bicep/generate/`, `pkg/cli/cmd/bicep/generate/templates/`, and `pkg/cli/cmd/bicep/generate/testdata/` with subdirectories `example-aspire-app/` (mirroring the reference application at `./example-aspire-app` with `infra/` and `AspireApp.AppHost/infra/` containing `.tmpl.yaml` files), `golden/`, `empty/`, `missing-ports/`, `multi-project/`

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Core data types, command skeleton, output templates, and test fixtures that ALL user stories depend on

**⚠️ CRITICAL**: No user story work can begin until this phase is complete

- [X] T002 Define all data model types in `pkg/cli/cmd/bicep/generate/models.go`: Parsed Model (AspireAppDescriptor, ServiceTemplate, IngressConfig, ContainerDef, EnvVar, SecretDef, BicepFile, BicepResource, BicepParameter, BicepModule, BicepVariable), Radius Model (RadiusApplication, RadiusContainer, RadiusPort, RadiusConnection, RadiusDependency, RadiusParameter, RadiusVariable), and Mapping Report (MappingEntry, MappingReport) — all fields, types, and json tags per data-model.md
- [X] T003 [P] Create Cobra command skeleton in `pkg/cli/cmd/bicep/generate/generate.go`: Runner struct, NewCommand(factory framework.Factory) function returning (*cobra.Command, framework.Runner), Validate method, flag registration (--from-aspire string required, --output string default ./app.bicep, --app-name string, --report string default ./mapping-report.md, --quiet bool, --deterministic bool), and register the `generate` subcommand in the parent bicep command group wiring
- [X] T004 [P] Create app.bicep Go text/template in `pkg/cli/cmd/bicep/generate/templates/app.bicep.tmpl`: header comment block (Generated by, Source, Date), `extension radius`, param declarations (environment, applicationName, per-service image params), Application resource (Applications.Core/applications@2023-10-01-preview), Container resources (Applications.Core/containers@2023-10-01-preview) with image/ports/env/connections, Dependency resources (Applications.Datastores/redisCaches@2023-10-01-preview, Applications.Datastores/sqlDatabases@2023-10-01-preview) — per cli-contract.md output format. Environment variables use `{ value: '<literal>' }` object syntax; dependency-backed env vars use Bicep resource expressions (e.g., `{ value: sqlserver.listSecrets().connectionString }`, `{ value: cache.properties.host }`, `{ value: string(sqlserver.properties.port) }`)
- [X] T005 [P] Create mapping-report.md Go text/template in `pkg/cli/cmd/bicep/generate/templates/mapping-report.md.tmpl`: header (Source, Generated, Output), Resource Summary table, per-resource Field-Level Mapping tables, Gaps table, Assumptions table — per cli-contract.md mapping report format
- [X] T006 [P] Create test fixture files replicating the reference application (`./example-aspire-app`) structure in `pkg/cli/cmd/bicep/generate/testdata/example-aspire-app/`: `infra/main.bicep` (subscription-scoped with environmentName, cache_password, sqlserver_password params), `infra/main.parameters.json`, `AspireApp.AppHost/infra/apiservice.tmpl.yaml` (Container App with ingress targetPort 8080/http, env including ConnectionStrings__weatherdb), `AspireApp.AppHost/infra/webfrontend.tmpl.yaml` (Container App with ingress targetPort 8080/http external:true, env including ConnectionStrings__cache and services__apiservice__http__0), `AspireApp.AppHost/infra/cache.tmpl.yaml` (Redis container port 6379/tcp), `AspireApp.AppHost/infra/sqlserver.tmpl.yaml` (SQL Server container port 1433/tcp)

**Checkpoint**: Foundation ready — user story implementation can now begin

---

## Phase 3: User Story 1 — Happy-Path Conversion and Deployment (Priority: P1) 🎯 MVP

**Goal**: Parse YAML templates and Bicep files from the reference application (`./example-aspire-app`) topology (webfrontend + apiservice + Redis + SQL Server), map to Radius model, and generate a valid app.bicep with Application resource, 2 Container resources, 1 Redis Portable Resource dependency, 1 SQL Database Portable Resource dependency, and correct connections (webfrontend→apiservice, webfrontend→cache, apiservice→sqlserver).

**Independent Test**: Run `rad bicep generate --from-aspire testdata/example-aspire-app/` and verify the output app.bicep matches the golden file. Golden file comparison passes byte-for-byte (with deterministic flag).

### Implementation for User Story 1

- [X] T007 [P] [US1] Implement YAML template + Bicep file parser in `pkg/cli/cmd/bicep/generate/parser.go`: ScanDirectory(dirPath) discovers the AppHost `infra/` directory containing `.tmpl.yaml` files and the solution-level `infra/main.bicep`; ParseYAMLTemplate(filePath) reads a `.tmpl.yaml` file, strips Go template expressions (`{{ .Image }}`, `{{ .Env.* }}`, `{{ securedParameter "..." }}`, `{{ targetPortOrDefault N }}`), parses the cleaned YAML, and extracts ServiceTemplate with IngressConfig, ContainerDef, EnvVar, SecretDef, and tags; ParseBicepFile(filePath) reads `main.bicep` and extracts parameters and modules using regexp
- [X] T008 [P] [US1] Implement Aspire-to-Radius mapper in `pkg/cli/cmd/bicep/generate/mapper.go`: MapToRadius(AspireAppDescriptor) returns (RadiusApplication, error); classifies each ServiceTemplate as service (http transport) or dependency (redis=port 6379/tcp, sqlserver=port 1433/tcp) using heuristics; mapContainer maps service templates to RadiusContainer with ImageParam ({name}Image), ImageDefault set to `IMAGE_PLACEHOLDER`, Ports from ingress, EnvVars from template.containers[0].env with dependency-backed env vars transformed to Bicep resource expressions (e.g., `ConnectionStrings__weatherdb` → `sqlserver.listSecrets().connectionString`, `WEATHERDB_HOST` → `sqlserver.properties.server`, `CACHE_HOST` → `cache.properties.host`); mapDependency maps Redis templates to RadiusDependency (Applications.Datastores/redisCaches, IsRecipeBacked:true) and SQL Server templates to RadiusDependency (Applications.Datastores/sqlDatabases, IsRecipeBacked:true); mapConnections derives connections from ConnectionStrings__ and services__ prefixed env vars
- [X] T009 [P] [US1] Implement Bicep output generator in `pkg/cli/cmd/bicep/generate/generator.go`: GenerateAppBicep(RadiusApplication, timestamp string) returns (string, error); loads and executes app.bicep.tmpl template; implement template helper functions — sortedEnvKeys for deterministic map iteration, bicepIndent for proper whitespace; env var rendering uses `{ value: '<literal>' }` syntax for plain values and unquoted Bicep expressions for dependency resource references; embed templates using Go embed directive
- [X] T010 [US1] Implement Runner.Run() method in `pkg/cli/cmd/bicep/generate/generate.go`: orchestrate full pipeline — call parser.ScanDirectory(fromAspirePath) to discover AppHost infra/ and solution infra/, parse YAML templates and main.bicep, call mapper.MapToRadius(descriptor), call generator.GenerateAppBicep(radiusApp, timestamp), write output string to --output path; print discovery summary to console: "Found N services: names", "Found N dependencies: names (type)"
- [X] T011 [P] [US1] Create golden file expected app.bicep in `pkg/cli/cmd/bicep/generate/testdata/golden/app.bicep` matching the reference application conversion result — must include extension radius, params for environment/applicationName/apiserviceImage/webfrontendImage (with `IMAGE_PLACEHOLDER` as default values), app resource (Applications.Core/applications@2023-10-01-preview), apiservice container (Applications.Core/containers@2023-10-01-preview) with sqlserver connection and env vars using Bicep resource expressions (`{ value: sqlserver.listSecrets().connectionString }` etc.), webfrontend container with apiservice+cache connections and env vars using Bicep resource expressions (`{ value: cache.properties.host }` etc.), cache Redis resource, and sqlserver SQL Database resource
- [X] T012 [P] [US1] Write parser unit tests (table-driven) in `pkg/cli/cmd/bicep/generate/parser_test.go`: test ScanDirectory discovers AppHost infra/ with 4 .tmpl.yaml files and solution infra/ with main.bicep; test ParseYAMLTemplate on apiservice.tmpl.yaml extracts ingress targetPort 8080, external:false, transport:http, and ConnectionStrings__weatherdb env var; test ParseYAMLTemplate on cache.tmpl.yaml extracts port 6379, transport:tcp; test ParseYAMLTemplate on sqlserver.tmpl.yaml extracts port 1433, transport:tcp; test Go template stripping handles {{ .Image }}, {{ securedParameter "..." }}, {{ targetPortOrDefault N }} correctly
- [X] T013 [P] [US1] Write mapper unit tests (table-driven) in `pkg/cli/cmd/bicep/generate/mapper_test.go`: test MapToRadius with parsed reference app data produces RadiusApplication with Name "example-aspire-app", 2 Containers (apiservice, webfrontend), 2 Dependencies (cache with type Applications.Datastores/redisCaches + sqlserver with type Applications.Datastores/sqlDatabases, both IsRecipeBacked:true), correct connections (webfrontend→apiservice, webfrontend→cache, apiservice→sqlserver), and 2 image parameters
- [X] T014 [P] [US1] Write generator unit tests with golden file comparison in `pkg/cli/cmd/bicep/generate/generator_test.go`: test GenerateAppBicep output for reference app RadiusApplication matches testdata/golden/app.bicep content; test output contains valid Bicep structure (extension radius, resource declarations, Applications.Core API versions, env vars with `{ value: }` syntax and Bicep resource expressions, sqlserver SQL Database resource)
- [X] T015 [US1] Write CLI command validation tests in `pkg/cli/cmd/bicep/generate/generate_test.go`: test NewCommand returns valid cobra.Command with all expected flags; test Validate succeeds with valid --from-aspire path pointing to example-aspire-app fixtures; test Validate fails with missing --from-aspire flag; use radcli SharedCommandValidation and ValidateInput test helpers where applicable

**Checkpoint**: User Story 1 complete — `rad bicep generate --from-aspire` produces a valid app.bicep from the reference application fixtures (including SQL Server as a `sqlDatabases` Portable Resource). This is the MVP deliverable.

---

## Phase 4: User Story 4 — Artifact-to-Resource Mapping Documentation (Priority: P2)

**Goal**: Produce a mapping report (console summary + companion mapping-report.md file) that traces every populated field in app.bicep back to its source Aspire artifact and field, and documents all assumptions made during conversion.

**Independent Test**: Run the conversion against reference app fixtures; verify mapping-report.md exists, contains a Resource Summary table with 5 resources (app + 2 containers + cache + sqlserver), field-level mapping tables for each resource where every populated field has a source entry (referencing `.tmpl.yaml` files), and an Assumptions section documenting image name defaults.

### Implementation for User Story 4

- [ ] T016 [US4] Implement reporter module in `pkg/cli/cmd/bicep/generate/reporter.go`: Reporter struct with Entries []MappingEntry; AddEntry(MappingEntry) method; GenerateConsoleReport(report MappingReport, w io.Writer) prints formatted summary to writer (resource count, connection count, gap count, assumption count per cli-contract.md console output format); GenerateMappingReportMarkdown(report MappingReport) returns (string, error) by executing mapping-report.md.tmpl template
- [ ] T017 [US4] Integrate mapping entry collection into mapper in `pkg/cli/cmd/bicep/generate/mapper.go`: modify MapToRadius to also return MappingReport; during each field mapping (app name, container name, image, ports, env vars, connections, dependency type), create a MappingEntry recording TargetResource, TargetField (dotted path e.g. container.ports.http.containerPort), SourceFile, SourceField, Value, and IsAssumption+GapMessage for defaults like image names
- [ ] T018 [US4] Integrate reporter output into Runner.Run() in `pkg/cli/cmd/bicep/generate/generate.go`: after generating app.bicep, call reporter.GenerateConsoleReport (unless --quiet flag set) and call reporter.GenerateMappingReportMarkdown then write result to --report path; update console output to include "Mapping report written to: <path>" and "Assumptions made: N"
- [ ] T019 [US4] Create golden file expected mapping report in `pkg/cli/cmd/bicep/generate/testdata/golden/mapping-report.md` with Resource Summary table (app, apiservice, webfrontend, cache, sqlserver), field-level mapping tables for each resource (referencing `.tmpl.yaml` source files), and Assumptions section for image name defaults
- [ ] T020 [P] [US4] Write reporter unit tests in `pkg/cli/cmd/bicep/generate/reporter_test.go`: test GenerateConsoleReport output format matches cli-contract.md console output (resource list with checkmarks, gap/assumption counts); test GenerateMappingReportMarkdown output matches testdata/golden/mapping-report.md; test all field-level entries present for reference app conversion (every populated field in app.bicep has a corresponding MappingEntry referencing a `.tmpl.yaml` source)
- [ ] T021 [US4] Update mapper tests for mapping entry verification in `pkg/cli/cmd/bicep/generate/mapper_test.go`: verify MapToRadius now returns MappingReport with Entries covering all mapped fields, each entry has correct SourceFile and SourceField values, image name entries are marked IsAssumption:true

**Checkpoint**: User Story 4 complete — conversion produces mapping-report.md and console summary tracing every field to its source artifact

---

## Phase 5: User Story 2 — Handling Missing or Ambiguous Aspire Artifacts (Priority: P2)

**Goal**: Detect missing or ambiguous data (ports, images, connections, unsupported dependency types), produce safe fallback placeholders with documented comments in app.bicep, emit clear error messages for invalid input directories, and integrate all gaps into the mapping report.

**Independent Test**: Run conversion against missing-ports fixture; verify app.bicep contains `/* PLACEHOLDER: port not found in Aspire artifacts — set manually */` comment and mapping-report.md has corresponding gap entry. Run against empty/ directory; verify command exits with FR-010 error message. Run against multi-project/ directory; verify command exits with FR-011 error message.

### Implementation for User Story 2

- [ ] T022 [P] [US2] Create error-path test fixture files: `pkg/cli/cmd/bicep/generate/testdata/empty/` (empty directory, no files), `testdata/missing-ports/AspireApp.AppHost/infra/apiservice.tmpl.yaml` (Container App YAML template WITHOUT ingress.targetPort property), `testdata/missing-ports/infra/main.bicep` (minimal main.bicep), `testdata/multi-project/AppHost1/infra/service1.tmpl.yaml` + `testdata/multi-project/AppHost2/infra/service2.tmpl.yaml` (two AppHost infra/ directories to trigger FR-011)
- [ ] T023 [US2] Implement input validation in `pkg/cli/cmd/bicep/generate/parser.go`: in ScanDirectory, validate directory exists and contains at least one AppHost `infra/` directory with `.tmpl.yaml` files — if none found return error "Input directory '<path>' does not contain Aspire infrastructure artifacts" (FR-010); validate exactly one AppHost infra directory — if multiple detected return error "Multiple Aspire projects detected" (FR-011) per cli-contract.md error messages
- [ ] T024 [US2] Implement gap detection in `pkg/cli/cmd/bicep/generate/mapper.go`: when ingress.targetPort not found in ServiceTemplate, create RadiusPort with IsPlaceholder:true and a MappingEntry with IsGap:true, GapMessage "Port not found in Aspire artifacts — set manually"; when container image is Go template expression (`{{ .Image }}`), use {name}:latest default and create MappingEntry with IsAssumption:true; when dependency type not recognized by heuristics (not Redis, SQL Server, or other supported type), create RadiusDependency with IsPlaceholder:true, PlaceholderComment explaining no Portable Resource equivalent
- [ ] T025 [US2] Implement placeholder rendering in `pkg/cli/cmd/bicep/generate/generator.go` and `pkg/cli/cmd/bicep/generate/templates/app.bicep.tmpl`: render placeholder ports with inline `/* PLACEHOLDER: port not found in Aspire artifacts — set manually */` comment; render unsupported dependencies as commented placeholder resource blocks with explanation; render configurable image params with @description noting the default is an assumption
- [ ] T026 [US2] Integrate gap entries into reporter in `pkg/cli/cmd/bicep/generate/reporter.go`: implement filterGaps() and filterAssumptions() on MappingReport to populate Gaps and Assumptions slices from Entries; in GenerateConsoleReport, print gap table listing each missing field with "Action Required" guidance; in GenerateMappingReportMarkdown, ensure Gaps and Assumptions tables render correctly in the template
- [ ] T027 [P] [US2] Write input validation error tests in `pkg/cli/cmd/bicep/generate/parser_test.go`: test ScanDirectory on testdata/empty/ returns error containing "does not contain Aspire infrastructure artifacts"; test ScanDirectory on nonexistent path returns clear error; test ScanDirectory on testdata/multi-project/ returns error containing "Multiple Aspire projects detected"
- [ ] T028 [P] [US2] Write gap detection and placeholder tests in `pkg/cli/cmd/bicep/generate/mapper_test.go`: test MapToRadius with missing-ports fixture produces RadiusContainer with placeholder port (IsPlaceholder:true) and MappingReport.Gaps contains port gap entry; test unsupported dependency type produces placeholder dependency
- [ ] T029 [US2] Write placeholder rendering tests in `pkg/cli/cmd/bicep/generate/generator_test.go`: test GenerateAppBicep with placeholder port renders PLACEHOLDER comment in output; test unsupported dependency renders commented placeholder resource block

**Checkpoint**: User Story 2 complete — conversion gracefully handles missing data with explicit placeholders, produces gap reports, and fails clearly on invalid input

---

## Phase 6: User Story 3 — Repeatable Conversion with Consistent Output (Priority: P3)

**Goal**: Guarantee byte-for-byte identical app.bicep and mapping-report.md output when running the conversion twice on identical input artifacts. Implement deterministic ordering rules and --deterministic flag for CI use.

**Independent Test**: Run conversion twice with --deterministic flag on identical reference app fixtures. Diff both app.bicep outputs — zero differences. Diff both mapping-report.md outputs — zero differences.

### Implementation for User Story 3

- [ ] T030 [US3] Implement deterministic ordering in `pkg/cli/cmd/bicep/generate/mapper.go` and `pkg/cli/cmd/bicep/generate/generator.go`: sort RadiusApplication.Containers by Name, sort RadiusApplication.Dependencies by Name, sort RadiusApplication.Parameters by Name before returning from MapToRadius; sort MappingReport.Entries by (TargetResource, TargetField); in generator template helpers, ensure EnvVars map keys are iterated in sorted order (extract keys to sorted slice)
- [ ] T031 [US3] Implement --deterministic flag handling in `pkg/cli/cmd/bicep/generate/generate.go`: when --deterministic is set, pass a fixed sentinel timestamp string "DETERMINISTIC" to GenerateAppBicep and GenerateMappingReportMarkdown instead of the current time, so output Date/Generated fields use the sentinel value
- [ ] T032 [US3] Write idempotency golden file tests in `pkg/cli/cmd/bicep/generate/generator_test.go`: run GenerateAppBicep twice with identical RadiusApplication input and deterministic timestamp, assert both outputs are byte-for-byte identical; assert output matches testdata/golden/app.bicep
- [ ] T033 [P] [US3] Write mapping report idempotency test in `pkg/cli/cmd/bicep/generate/reporter_test.go`: run GenerateMappingReportMarkdown twice with identical MappingReport input and deterministic timestamp, assert both outputs are byte-for-byte identical; assert output matches testdata/golden/mapping-report.md
- [ ] T034 [US3] Write end-to-end idempotency test in `pkg/cli/cmd/bicep/generate/generate_test.go`: run the full Runner.Run() pipeline twice with --deterministic flag against reference app fixtures, compare both app.bicep and mapping-report.md outputs, assert byte-for-byte identical

**Checkpoint**: User Story 3 complete — conversion is fully deterministic and automation-ready

---

## Phase 7: Polish & Cross-Cutting Concerns

**Purpose**: Flag handling refinements, code quality, documentation, and end-to-end validation

- [ ] T035 [P] Implement --quiet flag suppression of console mapping report output in `pkg/cli/cmd/bicep/generate/generate.go` (skip GenerateConsoleReport call when --quiet is set; mapping-report.md file is still written)
- [ ] T036 [P] Add godoc comments on all exported types and functions across `pkg/cli/cmd/bicep/generate/models.go`, `parser.go`, `mapper.go`, `generator.go`, `reporter.go`, and `generate.go` — each exported symbol must have a comment starting with the symbol name per Effective Go conventions
- [ ] T037 Run `gofmt` formatting and `make lint` compliance check across all files in `pkg/cli/cmd/bicep/generate/`; fix any linting issues
- [ ] T038 Run quickstart.md end-to-end validation: execute Steps 1–9 from `specs/004-aspire-to-bicep/quickstart.md` using the reference application at `./example-aspire-app` against the built CLI binary, verify app.bicep output (including sqlserver SQL Database resource), mapping report, deployment, and idempotency

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: No dependencies — can start immediately
- **Foundational (Phase 2)**: Depends on Phase 1 — BLOCKS all user stories
- **US1 (Phase 3)**: Depends on Phase 2 — core conversion pipeline (MVP)
- **US4 (Phase 4)**: Depends on Phase 3 — reporter integrates into existing pipeline
- **US2 (Phase 5)**: Depends on Phase 3 and Phase 4 — gap reporting uses reporter infrastructure
- **US3 (Phase 6)**: Depends on Phase 3 and Phase 4 — determinism applies to both app.bicep and mapping report
- **Polish (Phase 7)**: Depends on all user story phases being complete

### User Story Dependencies

- **User Story 1 (P1)**: Can start after Foundational (Phase 2) — **MVP deliverable**, no dependencies on other stories
- **User Story 4 (P2)**: Can start after US1 (Phase 3) — reporter integrates into mapper/runner from US1
- **User Story 2 (P2)**: Can start after US4 (Phase 4) — gap entries integrate into reporter from US4
- **User Story 3 (P3)**: Can start after US4 (Phase 4) — determinism applies to both outputs

### Within Each User Story

- Core components (parser, mapper, generator) before integration (Runner.Run)
- Implementation tasks before test tasks
- Golden files can be created in parallel with implementation
- Story complete and checkpoint verified before moving to next priority

### Parallel Opportunities

- **Phase 2**: T003, T004, T005, T006 can all run in parallel (different files, only depend on T002 models.go)
- **Phase 3**: T007, T008, T009 can run in parallel (parser, mapper, generator are separate files, depend only on models.go); T011, T012, T013, T014 can run in parallel (test files and golden files)
- **Phase 4**: T020 can run in parallel with T016–T019
- **Phase 5**: T022, T027, T028 can run in parallel
- **Phase 6**: T033 can run in parallel with T030–T032
- **Phase 7**: T035, T036 can run in parallel

---

## Parallel Example: User Story 1

```text
# Step 1 — Launch all core components in parallel (different files, depend only on models.go):
Task T007: "Implement YAML template + Bicep parser in parser.go"
Task T008: "Implement mapper in mapper.go"
Task T009: "Implement generator in generator.go"

# Step 2 — After components complete, wire them together:
Task T010: "Implement Runner.Run() in generate.go"

# Step 3 — Launch all tests and golden files in parallel:
Task T011: "Create golden file testdata/golden/app.bicep"
Task T012: "Write parser tests in parser_test.go"
Task T013: "Write mapper tests in mapper_test.go"
Task T014: "Write generator golden file tests in generator_test.go"

# Step 4 — After tests, integration test:
Task T015: "Write CLI command tests in generate_test.go"
```

---

## Implementation Strategy

### MVP First (User Story 1 Only)

1. Complete Phase 1: Setup (T001)
2. Complete Phase 2: Foundational (T002–T006)
3. Complete Phase 3: User Story 1 (T007–T015)
4. **STOP and VALIDATE**: Run `rad bicep generate --from-aspire testdata/example-aspire-app/` and verify app.bicep output matches golden file (including sqlserver SQL Database resource)
5. Deploy/demo if ready — this delivers a working conversion for the happy-path aspire-starter topology

### Incremental Delivery

1. Setup + Foundational → Foundation ready
2. Add User Story 1 → Test independently → **MVP! Working conversion for aspire-starter topology**
3. Add User Story 4 → Test independently → Adds mapping report tracing every field to its source
4. Add User Story 2 → Test independently → Adds error handling, gap reporting, and placeholders
5. Add User Story 3 → Test independently → Adds determinism guarantees for CI/automation
6. Polish → Final quality pass, godoc, lint, and end-to-end validation

### Parallel Team Strategy

With multiple developers:

1. Team completes Setup + Foundational together
2. Once Foundational is done:
   - Developer A: User Story 1 (blocks all other stories)
3. Once US1 is done:
   - Developer A: User Story 4 (reporter)
   - Developer B: User Story 2 test fixtures (T022) in parallel
4. Once US4 is done:
   - Developer A: User Story 3 (determinism)
   - Developer B: User Story 2 (remaining tasks — gap detection, placeholders, error handling)
5. Polish: Both developers

---

## Notes

- All paths relative to `radius` repository root (`github.com/radius-project/radius`)
- [P] tasks = different files, no dependencies on incomplete tasks in this phase
- [Story] label maps task to specific user story for traceability
- Each user story is independently testable at its checkpoint
- Commit after each task or logical group
- Stop at any checkpoint to validate story independently
- Tests are included per plan.md project structure (unit tests, golden file tests, integration tests)
- The PoC targets a fixed topology: 2 services (frontend + api) + 1 Redis dependency + 1 SQL Server dependency
